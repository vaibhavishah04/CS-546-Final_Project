/**
 * sensors.js
 * 
 * Tasks:
 * 1. Add, retrieve, update, and delete sensor data.
 * 2. Allow search functionality by sensor name or location.
 * 
 * Functions:
 * - addSensor(sensorData): Add a new sensor.
 * - getAllSensors(): Retrieve all sensors.
 * - getSensorByIdOrName(identifier): Retrieve a sensor by ID or name.
 * - updateSensor(sensorId, updateData): Update sensor details.
 * - deleteSensor(sensorId): Remove a sensor from the database.
 */
